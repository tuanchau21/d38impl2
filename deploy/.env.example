# d38shop — backend production environment template
# Copy to implementation/backend/.env on the server. Do not commit real values.
# (Frontend may use API_URL or NEXT_PUBLIC_API_URL in implementation/frontend/.env for SSR.)

# -----------------------------------------------------------------------------
# Node
# -----------------------------------------------------------------------------
NODE_ENV=production

# -----------------------------------------------------------------------------
# Database (MySQL HeatWave — Oracle Cloud Always Free)
# -----------------------------------------------------------------------------
# Option A: single URL
DATABASE_URL=mysql://USER:PASSWORD@HOST:3306/DATABASE

# Option B: separate variables (use if your app reads these instead)
# MYSQL_HOST=
# MYSQL_PORT=3306
# MYSQL_USER=
# MYSQL_PASSWORD=
# MYSQL_DATABASE=

# -----------------------------------------------------------------------------
# Oracle Object Storage (S3-compatible API)
# -----------------------------------------------------------------------------
# Bucket and region
OCI_OBJECT_STORAGE_NAMESPACE=
OCI_OBJECT_STORAGE_BUCKET=
OCI_OBJECT_STORAGE_REGION=

# S3-compatible credentials (from OCI customer secret key / API key)
OCI_OBJECT_STORAGE_ACCESS_KEY=
OCI_OBJECT_STORAGE_SECRET_KEY=

# Optional: custom endpoint if not using default (e.g. https://objectstorage.REGION.oraclecloud.com)
# OCI_OBJECT_STORAGE_ENDPOINT=

# -----------------------------------------------------------------------------
# Admin API protection
# -----------------------------------------------------------------------------
# API key for /api/admin/* (e.g. X-Admin-Key header). Required in production.
ADMIN_API_KEY=

# -----------------------------------------------------------------------------
# (Backend does not need APP_URL. Frontend uses API_URL for SSR calls to backend.)
# -----------------------------------------------------------------------------
# In implementation/frontend/.env (optional): API_URL=http://127.0.0.1:3001
# so the frontend can call the backend during server-side rendering.

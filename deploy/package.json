{
  "name": "d38shop-deploy",
  "version": "0.1.0",
  "private": true,
  "description": "Deploy scripts (seed-admin, manage-admin-users). Run npm install here once, then node deploy/script.js from repo root.",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "mysql2": "^3.11.0"
  }
}
